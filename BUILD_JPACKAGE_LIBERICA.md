# 📦 Создание Self-Contained Установщика со встроенной Liberica JDK

## Что это?

**Self-contained installer** - это установщик, который включает в себя **всё необходимое**, включая Java runtime. Пользователям **НЕ нужно устанавливать Java** отдельно!

### Преимущества:

- ✅ **Встроенная Java** - не нужно устанавливать JRE/JDK отдельно
- ✅ **Полная изоляция** - своя версия Java, не конфликтует с другими приложениями
- ✅ **Простота** - двойной клик для установки, как обычная Windows программа
- ✅ **Меню Пуск** - автоматически добавляется ярлык
- ✅ **Удаление** - через стандартное "Установка и удаление программ"

### Размер:
- **С Liberica JDK**: ~60-80 MB (включает Java runtime)
- **Launch4j версия**: ~13 MB (требует установленную Java)

---

## 🚀 Инструкция по созданию

### Шаг 1: Установить Liberica JDK 17 Full

**Важно**: Нужна именно **Full** версия (не Standard), так как только Full включает `jpackage`.

1. Перейдите на: https://bell-sw.com/pages/downloads/#jdk-17-lts
2. Выберите:
   - **Version**: 17 LTS
   - **OS**: Windows
   - **Architecture**: x86-64 (или arm64 если у вас ARM процессор)
   - **Package**: **Full JDK** (не Standard!)
3. Скачайте установщик (например: `bellsoft-jdk17.0.9+11-windows-amd64-full.msi`)
4. Запустите установщик
5. Убедитесь что установлен в PATH:

```cmd
java -version
jpackage --version
```

Если `jpackage --version` работает - всё готово!

---

### Шаг 2: Собрать проект

```cmd
cd C:\path\to\onlyjirasvetofor
mvn clean package
```

Это создаст файлы в папке `target/`:
- `svetoofor-client.jar`
- `svetoofor-server.jar`
- `svetoofor-installer.jar`

---

### Шаг 3: Настроить client.properties

**ВАЖНО**: Перед созданием установщика **обязательно** укажите правильный адрес сервера!

Отредактируйте файл `client.properties`:

```properties
# Адрес вашего сервера FUDO
server.address=ws://10.10.90.170:52521
server.port=52521

# Учетные данные админа
admin.login=admin
admin.password=qweasd123#$
```

---

### Шаг 4: Создать установщик

**Вариант A: Двойной клик (самый простой)**

```cmd
cd installer
build-jpackage.bat
```

Просто дважды кликните на файл `build-jpackage.bat`

**Вариант B: Через командную строку**

```cmd
cd installer
build-jpackage.bat
```

Процесс займёт **2-5 минут**. Вы увидите:

```
================================================
Building self-contained Windows installer...
This may take 2-5 minutes...
================================================

[OK] Creating application image...
[OK] Packaging installer...

SUCCESS! Self-contained installer created:
jpackage-output\Svetoofor-JIRA-Client-1.0.0.exe
```

---

### Шаг 5: Готово!

Установщик создан в папке:

```
installer\jpackage-output\Svetoofor-JIRA-Client-1.0.0.exe
```

Размер: **~60-80 MB** (включает Liberica JDK runtime)

---

## 📋 Что делает установщик?

При запуске `Svetoofor-JIRA-Client-1.0.0.exe` на компьютере пользователя:

1. ✅ Мастер установки (Next → Next → Install)
2. ✅ Устанавливает в `C:\Program Files\Svetoofor-JIRA-Client\`
3. ✅ Копирует приложение + встроенную Java
4. ✅ Создаёт ярлык в меню Пуск
5. ✅ Создаёт деинсталлятор
6. ✅ Регистрирует в "Программы и компоненты"

**Java НЕ нужна!** Всё уже встроено.

---

## 🖥️ Использование установщика

### Для администраторов:

**Шаг 1**: Положите установщик в сетевую папку

```
\\fudo\share\Svetoofor-JIRA-Client-1.0.0.exe
```

**Шаг 2**: Отправьте инструкцию пользователям:

> 1. Откройте `\\fudo\share\Svetoofor-JIRA-Client-1.0.0.exe`
> 2. Нажмите "Next" → "Next" → "Install"
> 3. Запустите "Svetoofor JIRA" из меню Пуск

### Для удаления:

**Панель управления** → **Программы и компоненты** → **Svetoofor-JIRA-Client** → **Удалить**

---

## ⚙️ Настройка установщика

Если нужно изменить параметры установки, отредактируйте `installer/build-jpackage.bat`:

```batch
REM Название приложения
--name "Svetoofor-JIRA-Client"

REM Версия
--app-version 1.0.0

REM Компания
--vendor "ITSMJIRA"

REM Описание
--description "Traffic Light JIRA Client"

REM Меню Пуск
--win-menu-group "Svetoofor JIRA"
```

После изменений запустите скрипт заново.

---

## 🆚 Сравнение методов

| Метод | Размер | Требует Java? | Установка | Удаление |
|-------|--------|---------------|-----------|----------|
| **jpackage + Liberica** | ~70 MB | ❌ НЕТ | Мастер установки | Через "Программы" |
| **Launch4j EXE** | ~13 MB | ✅ ДА | Запуск GUI | Вручную |
| **Inno Setup** | ~13 MB | ✅ ДА | Мастер установки | Через "Программы" |
| **JAR файл** | ~13 MB | ✅ ДА | java -jar | Вручную |

**Рекомендация**: Используйте **jpackage + Liberica** для максимального удобства пользователей!

---

## ❌ Troubleshooting

### "jpackage not found"

**Решение**:
1. Установите **Liberica JDK 17 Full** (не Standard!)
2. Проверьте PATH: `echo %JAVA_HOME%`
3. Добавьте в PATH: `%JAVA_HOME%\bin`
4. Перезапустите командную строку
5. Проверьте: `jpackage --version`

### "svetoofor-client.jar not found"

**Решение**:
```cmd
cd ..
mvn clean package
cd installer
build-jpackage.bat
```

### Установщик не запускается

**Решение**:
1. Запустите от имени администратора
2. Проверьте антивирус (может блокировать)
3. Проверьте что Windows 10/11 (Windows 7 не поддерживается)

### Ошибка "WiX Toolset required"

**Решение**:
1. Скачайте WiX Toolset: https://wixtoolset.org/releases/
2. Установите WiX 3.11 или новее
3. Перезапустите командную строку
4. Попробуйте снова

---

## 🎯 Готовые команды для копипаста

### Полный процесс сборки (Windows):

```cmd
REM 1. Собрать проект
cd C:\path\to\onlyjirasvetofor
mvn clean package

REM 2. Создать установщик
cd installer
build-jpackage.bat

REM 3. Найти установщик
dir jpackage-output\*.exe
```

### Проверка что всё установлено:

```cmd
java -version
jpackage --version
mvn -version
```

---

## 📚 Дополнительная информация

### Официальная документация:

- **jpackage**: https://docs.oracle.com/en/java/javase/17/jpackage/
- **Liberica JDK**: https://bell-sw.com/pages/libericajdk/
- **Maven**: https://maven.apache.org/

### Альтернативы:

Если jpackage не работает, используйте:
- **Inno Setup** (см. `BUILD_INSTALLER.md`)
- **Launch4j** (см. `installer/build-exe.bat`)

---

## ✅ Чек-лист перед сборкой

- [ ] Установлен Liberica JDK 17 Full
- [ ] `jpackage --version` работает
- [ ] Проект собран (`mvn clean package`)
- [ ] Файл `client.properties` настроен (адрес сервера FUDO)
- [ ] Иконка существует (`src/main/resources/44_85245.ico`)

---

**Успешной сборки! 🚀**

Если возникнут проблемы, проверьте:
1. Версия JDK (должна быть Full, не Standard)
2. Путь к jpackage в PATH
3. Все файлы на месте
4. Windows 10/11 (не Windows 7)
